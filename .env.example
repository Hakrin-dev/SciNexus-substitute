# 研枢 SciNexus 部署环境变量示例
# 复制为 .env / .env.local 后按需填写

# 前端访问后端的地址（本地开发默认；线上改为后端公网地址）
# 本地：http://localhost:8000（backend/ 目录 uvicorn 启动）
# 线上示例：https://api.example.com 或 http://47.76.187.249:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# NextAuth 认证(规划中,接入时再填)
# AUTH_SECRET=

# 数据库连接(规划中,接入时再填)
# DATABASE_URL=
