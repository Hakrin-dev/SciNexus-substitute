import { NewProjectFlow } from "@/components/features/projects/new-project-flow";

/** 新建课题 `/projects/new` —— 三步向导(前端演示原型) */
export default function NewProjectPage() {
  return <NewProjectFlow />;
}
